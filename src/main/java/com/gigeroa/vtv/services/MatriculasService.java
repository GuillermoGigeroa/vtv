package com.gigeroa.vtv.services;

/*
 * Servicio creado para poder administrar las validaciones.
 */

public abstract class MatriculasService {
	public static void verificarNuevaMatricula (String matricula) {
		String matriculaActual = matricula.toLowerCase().trim();
		for (int x = 0; x < matriculaActual.length(); x++) {
			
		}
	}
}
